You are Session A (Claude Code, opus) on the Komun repo. Worktree: /workspace, branch feature/agent-a.

Read these first, in order:
  1. /workspace/session_tasks.md          <- your scope contract (what you may write, may run, definition of done)
  2. /workspace/.dispatch/SPEC.md          <- the frozen spec. Part 1.4 is the target schema. Part 5.1 is your write set.

This dispatch is WAVE A1 only: fresh schema baseline, core models, the enum<->CHECK pin test, and the
relay deletion. Do NOT start A2/A3/A6 in this run, and do not touch web/.

TASK

1. A1.1 — Replace the migrations directory.
   - FIRST read migrations/014_post_fts.sql and migrations/001-015 in full: you must carry over every
     index, every constraint and the posts_search_update() trigger into the new file.
   - Delete migrations/001_schema.sql through migrations/015_community_images.sql.
   - Create migrations/001_schema.sql implementing SPEC Part 1.4 exactly: tables, columns, CHECK lists,
     FKs (posts.category -> categories(slug) ON DELETE RESTRICT), UNIQUEs, all carried-over indexes, the
     tsvector column + trigger + GIN index, and the 23 seeded categories rows from SPEC Part 1.6.
   - No other migration file may exist afterwards.

2. A1.2 — Core models.
   - Delete crates/core/src/models/community.rs and member.rs (and their mod declarations).
   - user.rs: email/password oriented, typed Role enum (user/admin/superadmin), the wrapped-bundle
     fields from SPEC Part 1.4, and NO public_key / recovery_id / recovery_code_hash.
   - post.rs: drop community_id, collapse Visibility to public|private, add the marketplace columns
     (market_listed, price_cents, currency, price_negotiable, item_condition, sold_at, buyer_id) and
     PostKind::Want.
   - match_thread.rs: Message carries ciphertext + nonce, not body.
   - tests.rs: drop the community tests (this also removes the image_path compile breakage at line 135).

3. A1.3 — Pin enum <-> schema agreement (the test class that was missing).
   - In crates/core/src/tests.rs, for every enum that maps to a DB text column (PostKind, Urgency,
     PostStatus, Visibility, Role, and ItemCondition/OfferKind if you introduce them) assert that
     as_str()'s value set EQUALS the CHECK (col IN (...)) list parsed out of migrations/001_schema.sql.
     Parse the migration file at test time; do not hardcode a second copy of the list.
   - Category is no longer an enum: give it its own test instead — every seeded row's scope is one of
     the CHECK values, every slug is unique lowercase-kebab, no label is empty.

4. A1.4 — Delete the relay.
   - crates/relay/** , crates/server/src/relay_bridge.rs , crates/server/src/relay_ops.rs ,
     data/relay/ , the [relay] config section in config.rs, the komun-relay dependency and the
     workspace member in Cargo.toml, and the relay spawn block in main.rs (roughly lines 70-91).
   - Remove AppState.relay_store and anything that only existed to serve the relay. Mainland rule: after
     this, `grep -ri piggpin .` must be empty outside .git/target.

FILES YOU MAY WRITE (and nothing else)
  migrations/** , crates/core/src/** , crates/relay/** (delete) , crates/server/src/relay_bridge.rs
  + relay_ops.rs (delete) , crates/server/src/main.rs , crates/server/src/config.rs ,
  Cargo.toml , Cargo.lock , crates/wasm/Cargo.toml , crates/server/Cargo.toml , crates/relay/Cargo.toml
NOT in this run: crates/server/src/auth/** , api/** , db/** (A2/A3), anything under web/.
If config.rs or main.rs needs a change that is not part of the relay deletion, stop and say so — do not
widen scope.

STANDING RULES
1. Touch only the files listed above. If another file must change, write the exact unified diff into
   /workspace/.dispatch/hub-requests/A1.md and continue with the rest. Never edit that other file.
2. NEVER run git commit, push, checkout, restore, stash, rebase, reset or worktree. Read-only git
   (status/diff/log/show) is fine. The orchestrator owns git.
3. NEVER add a dependency and never run cargo add / npm install / anything that needs the network: this
   container has no internet.
4. Write each file the moment it is ready. Do not batch writes at the end.
5. Run the gates below yourself and paste RAW output. If a gate fails, fix it and re-run. If you cannot
   fix it, say exactly what fails, with the error text.
6. Never report success you did not observe. A file that exists but does not compile is a failure.
7. On any provider/billing error, stop and report — do not retry in a loop.

GATES (run these; paste raw output)
  export PATH=/usr/local/cargo/bin:$PATH   # a login shell drops cargo from PATH; do not skip this
  cd /workspace
  cargo test -p komun-core
  cargo build --workspace
  grep -ri piggpin . --exclude-dir=.git --exclude-dir=target | wc -l          # expect 0
  grep -c "communities\|members" migrations/001_schema.sql                    # expect 0
  git status --short ; git diff --stat                                        # scope check
  # your database is up on agent-net (SPEC Part 4.0.2); DATABASE_URL is NOT exported in this
  # container, so use the literal connection string:
  psql "postgres://komun:komun@komun-db-a:5432/komun_a" -c "\dt"
  psql "postgres://komun:komun@komun-db-a:5432/komun_a" -f migrations/001_schema.sql   # empty DB only
  # then prove these writes are REJECTED by the schema (each must fail):
  #   negative price | market_listed=true on kind need | lowercase currency | item_condition='mint'
  #   rating 6 | duplicate review | offer kind 'bid' | role='root' | visibility='federated'
  #   duplicate email differing only by case | unknown category slug (FK) | DELETE a category in use (RESTRICT)
  # finally, prove the A1.3 test is real: delete one CHECK entry by hand, watch the test fail, restore it.
If the database is unreachable because Wave 0.2 has not run yet, run every code-level gate and report
`BLOCKED: database unreachable` — do not invent schema results.

END YOUR FINAL MESSAGE WITH EXACTLY:
  FILES WRITTEN: <paths>
  GATES RUN: <command -> observed result>
  BLOCKED: <none, or the precise blocker>
