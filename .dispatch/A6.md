# CARD A6 — Frontend flatten (the last card that turns the frontend green)

**Owner:** Agent A (Claude Code/opus) · **Branch:** `feature/agent-a` · **Worktree:** `/home/computing/repo-agent-a` (container `agent-repo-agent-a`, cwd `/workspace`)
**Depends on:** A3 (DONE, verified — the backend is flat: `/api/posts`, no community anywhere, conversations sealed). **Blocks:** B4's docs, Phase B's frontend.

## What changed in your tree BEFORE this card — do not redo, do not revert
Your branch was fast-forwarded onto Session B's work (`1ba30f2`) and B's branch was merged with yours; the two
branches now hold IDENTICAL trees, and every gate below was measured by the orchestrator on this exact tree.
You now have, already reviewed and verified: B's geocode hardening (`crates/server/src/api/geocode/{mod,limiter,cache}.rs`
plus `[geocode]` in `config.rs`), the Leaflet map (`web/src/routes/map/**`,
`web/src/lib/components/LocationMap.svelte`, `leaflet` in `package.json`, OSM hosts allowed in
`web/svelte.config.js` — that file is B's, file a hub request if your work needs a CSP change), the docs
`docs/CONVENTIONS.md` + `docs/DEVELOPMENT.md`, B's card `.dispatch/B5.md`, and B's DELETIONS: the whole
federation and alliances backend. `cargo clippy -p komun-server --no-deps` is **0 rows** and
`cargo clippy --release -- -D warnings` PASSES — that is the new floor, not 5, and this card must not
raise it. `web/node_modules/` is refreshed (leaflet installed) and `crates/wasm/pkg/` is current.
Measured here and now: build `--all-targets` clean; `cargo test --workspace` core 20 + server 58 passed,
0 failed; `npm run check` 14 errors / 37 warnings; `npm run build` green; `npx vitest run`
1 failed | 46 passed (47) — the single failure is still `AidCard > shows community name and server`.
A6 is the reverse merge later: the orchestrator merges THIS card's result back into `feature/agent-b`.
Never rebase, never force-push, and never `git checkout` a file to 'sync' the branches.

## STANDING RULES (unchanged)
1. No git mutations. No `cargo add` / `npm install` / network fetch. No docker, no sudo.
2. **You never delete or move files** — list them under `DELETIONS REQUESTED:` and the orchestrator executes it. This card needs a batch of deletions (A6.1); enumerate them exactly.
3. Writes confined to `/workspace`. Never touch Session B's DB (`komun_b`) or its worktree.
4. A warning/lint claim must come from the command that emits it.
5. Report ends with `FILES WRITTEN: / GATES RUN: / BLOCKED: / DELETIONS REQUESTED:`.

## A6.0 — Two defects in your OWN harness, fix them first (they cost the orchestrator a verification round)
Both are in `.dispatch/A3-live.mjs` and both produced false results when it was re-run:
1. **Count-based assertions are not idempotent.** `feed has both posts` and `search finds the need` assert on the TOTAL number of rows in the shared tables, so a second run against the same database fails on the fixtures the first run left behind (observed: `feed has both posts — 6 entries`). Assert on the ids THIS run created — the feed must CONTAIN them, not equal a count.
2. **The DB-side assertions ignore the server's configured database.** `sql()` runs `psql` against a hardcoded connection (`komun_a`), so when the server is pointed at another database every DB claim silently queries the wrong one (observed: `matches.message is NULL` FAILED on a fresh DB purely because the id it names does not exist there — the value is genuinely NULL, the query just looked in the wrong place). Derive the connection from the same source the server uses, or pass it in.
Fix both, then re-run and report the numbers. A harness that cannot be re-run is worth less than its output.

## A6.1 — Delete the community frontend
Request the deletion of: `web/src/routes/c/**`, `web/src/routes/community/**`, `web/src/routes/federation/**`, `web/src/routes/admin/communities/**`. Create `web/src/routes/p/[id]/+page.svelte` (post detail, reached as `/p/{id}`). Strip the slug parameter from `+layout.svelte`, `+page.svelte`, `aid/**`, `search`, `admin`, `users/[id]`, `account`, `connect`. `RespondModal.svelte` was repaired by the orchestrator (`register` → `/account/signup`) — rewrite it properly to use the session API now that you own that file's shape.

## A6.2 — The API client
`web/src/lib/api/**`: the `communities` group becomes a flat `posts` group (`list`, `get`, `create`, `update`, `delete`, `addImages`); drop every community/admin-community and alliance call. Keep the group's function names stable where the pages already call them, or update the callers in the same card — no half-migrated state.

## A6.3 — The tests that encode the old model
`AidCard.test.ts` loses its community props, and the 12 errors in it plus the 2 in `web/src/routes/c/[slug]/p/[id]/+page.svelte` disappear with the flattening. The single failing test (`AidCard > shows community name and server`) must become a test of the flattened card, not be deleted to make the count green — if a test's subject no longer exists, replace it with one asserting the new shape.

## Files you own
`web/src/routes/**` (except `web/src/routes/map/**`, which B2 created), `web/src/lib/api/**`, `web/src/lib/components/**` (existing ones), `web/src/tests/**`, `.dispatch/A3-live.mjs` (for A6.0).
Read-only: `crates/**` (the backend is frozen until Phase B), `web/src/routes/map/**`.

## Gate — this is the card where the frozen baseline must reach ZERO
```bash
docker exec -w /workspace agent-repo-agent-a bash -c 'export PATH=/usr/local/bin:/usr/local/cargo/bin:/usr/bin:/bin; cd /workspace
grep -rn "slug" web/src ; echo "^^ expect EMPTY"
cd web && npm run check && npm run build && npx vitest run'
```
- **`grep -rn "slug" web/src` → empty.** Today it is not.
- **`npm run check` → 0 errors.** Baseline history: 14 errors / 37 warnings in 8 files at A2b (12 in `AidCard.test.ts`, 2 in `c/[slug]/p/[id]/+page.svelte`, all 37 warnings community UI). This card deletes or rewrites everything that produces them, so **the criterion changes from "no new diagnostic" to "zero errors"** — the first card in the project where that is reachable. Warnings: drive them down with the same work; if any survive, name each one and the file.
- **`npm run build` → green**, `web/build/` produced.
- **`npx vitest run` → 0 failures.** Today on this tree: **1 failed | 46 passed (47)** (the count grew from 43 when the merge brought B's `map.test.ts` and A's crypto tests into one tree). The failure must disappear because its subject was replaced, not because the test was deleted.
- If a route cannot load against a running server, that is a finding — boot the server on port 3011 (copy `/workspace/config.toml`, change the port, run from a scratch cwd; `komun_a` is bookmarked and starts) and load the new routes.

## Exit criteria
`npm run check` 0 errors, `npm run build` green, `npx vitest run` 0 failed, `grep -rn "slug" web/src` empty, the deleted-route list stated exactly under `DELETIONS REQUESTED:`, and `FRONTEND-BASELINE*.md` retired — it exists only to describe inherited red, and this card ends that. You touch no Rust file, so the backend gates must be UNCHANGED: build `--all-targets` clean, core 20 + server 58, clippy 0 rows, `clippy --release -- -D warnings` still passing. Re-run them and report the raw numbers anyway.

## Orchestrator hand-off notes
- `web/node_modules/` is installed and `crates/wasm/pkg/` is current, so all three frontend gates run in this container. **The orchestrator rebuilds `crates/wasm/pkg/` if you touch `crates/wasm/**` — you cannot (`wasm-pack` needs a target and egress this container lacks).** Nothing in this card should need a wasm change; if it does, file it.
- Backend state you can rely on: `/api/posts` (feed, one, create, patch, delete, images), `/api/search` (no community join, tags are `TEXT[]`), `/api/conversations/**` (sealed ciphertext + nonce only), `/api/me/conversations`, `/api/auth/**` (session tokens), `/api/alliances` 404s. Verified live with raw bodies.
- The nav "Join" control is an `<a>` styled by `.join-link`, which was written for a `<button>`. The user was asked and chose to apply the CSS: `display: inline-block; text-decoration: none` is already in `+layout.svelte` with a comment saying why. If your rewrite replaces that control or its class, carry that rule over; do not silently drop it. `+layout.svelte` needs no other cosmetic change.
