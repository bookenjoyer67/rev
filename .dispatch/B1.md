You are Session B (opencode, deepseek-v4-flash) on the Komun repo. Worktree: /workspace, branch feature/agent-b.

Read these first, in order:
  1. /workspace/session_tasks.md          <- your scope contract (what you may write, may run, definition of done)
  2. /workspace/.dispatch/SPEC.md          <- the frozen spec. Part 5.2 is your write set, Part 1.5 is the config shape.

This dispatch is WAVE B1 only: harden the Nominatim geocode proxy (SPEC task A4.2). It is deliberately
independent of Session A's schema work. Do NOT touch the map/Leaflet UI, docs or deploy files in this run.

TASK

1. A 1 request/second limiter for outbound Nominatim calls, honouring Nominatim's usage policy. It must
   QUEUE (await) the second request rather than dropping it or firing it early. Key it on the process, not
   the user: one limiter for the whole proxy.
2. A result cache for geocode lookups, keyed on the normalised query, with a bounded size or TTL so it
   cannot grow without limit. A cache hit must not consume a limiter slot's usefulness (i.e. serve from
   cache without an upstream call).
3. A User-Agent built from an operator-supplied contact instead of the hardcoded "Komun/0.1". Per
   Nominatim policy the header must identify the deployment and give a contact. Add to config.rs, via hub
   request (below): a [geocode] section with `contact` (email or URL) and `user_agent` derived from it, and
   sane defaults that keep the current behaviour when unset.
4. No new dependency. This container has no network, so anything not already in Cargo.toml is impossible —
   implement the limiter with tokio/async primitives already in the tree (e.g. tokio::time, an async Mutex),
   and the cache with an existing structure or a small hand-rolled map + timestamps.
5. Keep the change inside the geocode module. You may keep it in crates/server/src/api/geocode.rs, or
   convert that file into crates/server/src/api/geocode/ with mod.rs + limiter.rs + cache.rs — that needs
   NO edit to api/mod.rs, because `mod geocode;` resolves to either form. Do not move geocode to a new
   top-level module name: that would need main.rs, which is Session A's file.

FILES YOU MAY WRITE (and nothing else)
  crates/server/src/api/geocode.rs   (or crates/server/src/api/geocode/** if you split it)
  /workspace/.dispatch/hub-requests/B1.md   <- how you request the config.rs change
Everything else is read-only for you, including crates/server/src/config.rs and config.example.toml.

THE HUB REQUEST (required, not optional)
config.rs is Session A's file. Write .dispatch/hub-requests/B1.md containing:
  - the file path and the exact unified diff adding the [geocode] section (struct + defaults + the doc
    comment saying the contact must be a real address because Nominatim requires it),
  - the reason,
  - the gate command that proves it works after it is applied.
Then keep working on everything that does not depend on that one field, and say in your report that the
config field is pending. Do not edit config.rs yourself.

STANDING RULES
1. Touch only the files listed above. If another file must change, write the exact unified diff into
   /workspace/.dispatch/hub-requests/B1.md and continue with the rest. Never edit that other file.
2. NEVER run git commit, push, checkout, restore, stash, rebase, reset or worktree. Read-only git
   (status/diff/log/show) is fine. The orchestrator owns git.
3. NEVER add a dependency and never run cargo add / npm install / anything that needs the network.
4. Write each file the moment it is ready. Do not batch writes at the end.
5. Run the gates below yourself and paste RAW output. If a gate fails, fix it and re-run. If you cannot
   fix it, say exactly what fails, with the error text.
6. Never report success you did not observe. A file that exists but does not compile is a failure.
7. On any provider/billing error, stop and report — do not retry in a loop.

GATES (run these; paste raw output)
  export PATH=/usr/local/cargo/bin:$PATH   # a login shell drops cargo from PATH; do not skip this
  cd /workspace
  cargo build --workspace
  cargo clippy --release -- -D warnings
  cargo test --workspace
  grep -rn "Komun/0.1" crates/server/src ; echo "^^ must be empty after your change"
Write the tests the definition of done names:
  - a test that two immediate lookups do NOT both reach upstream (the second is queued/delayed),
  - a test that the User-Agent contains the configured contact (and the default when unset),
  - a test that a cache hit does not call upstream again,
  - a boot check with a nonsense contact value still starting clean.
If a human-readable contact cannot resolve because the config field is still pending, assert against the
default value and say so explicitly.

END YOUR FINAL MESSAGE WITH EXACTLY:
  FILES WRITTEN: <paths>
  GATES RUN: <command -> observed result>
  BLOCKED: <none, or the precise blocker>
