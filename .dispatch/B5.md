# CARD B5 — Decouple advertised registration from the signup policy

**Owner:** Agent B (opencode) · **Branch:** `feature/agent-b` · **Worktree:** `/home/computing/repo-agent-b` (container `agent-repo-agent-b`, cwd `/workspace`)
**Depends on:** nothing outstanding — B3 is merged on your branch. **Independent of A's remaining cards.**

## The decision (made by the user, not by you or by me)
A3 made `api/directory.rs` gate peer registration on `config.registration.mode`, and the discovery config no
longer carries any registration flag — so today one setting does two jobs: **whether this server lets people
sign up**, and **whether it advertises itself / accepts peers as openly registerable**. The user has chosen to
separate them with a distinct `discovery.open_registration` boolean. Implement exactly that; do not invent a
different shape or rename the concept.

## Task

**B5.1 — the flag.** Add to `DiscoveryConfig` in `crates/server/src/config.rs` (your file since B3):
```rust
/// Whether this server advertises itself as openly registerable, and whether it accepts
/// peer registrations into its directory. Deliberately SEPARATE from `[registration] mode`,
/// which governs user signup only.
pub open_registration: Option<bool>,
```
plus one method that resolves it: an explicit value wins; when unset it falls back to
`config.registration.mode == "open"`, so every existing config keeps its current behaviour. Use that method
everywhere you need the value — never `registration.mode` directly.

**B5.2 — the gate.** `api/directory.rs`'s `registration_is_open` becomes the resolved flag. A server whose users
sign up by invite only must still be able to accept peer registrations when the operator says so — that is the
bug this card fixes.

**B5.3 — what is advertised.** `tasks/registration.rs` includes `open_registration` in the payload it POSTs to
`directory_url`; `api/node.rs` includes the resolved value in the node/discovery response. Widen
`RegisterRequest` in `api/directory.rs` to accept the field with `#[serde(default)]` so an older peer that does
not send it still registers.

**B5.4 — store what peers advertise.** Add a NEW migration `migrations/002_directory_open_registration.sql`
adding `open_registration BOOLEAN NOT NULL DEFAULT true` to `directory_entries`, and read/write it in
`register_server` and the entry queries.
**DO NOT EDIT `migrations/001_schema.sql`.** Its sha384 is bookmarked in `komun_a`'s `_sqlx_migrations` (the
schema was hand-loaded, verified by the orchestrator); changing one byte of that file makes every boot of
`komun_a` fail with a checksum mismatch. An additive 002 is the only safe shape, and if you touch 001 the
runtime gate below will fail for a reason that has nothing to do with your code.

**B5.5 — `config.example.toml` is YOURS (one writer).** Add `open_registration` under `[discovery]` with a
comment stating that leaving it out means "follow `[registration] mode`". This is also the file B4 documents,
so keep the wording you will want in the docs.

## Standing rules
1. No git mutations. No `cargo add` / `npm install` / network fetch. No docker, no sudo.
2. You never delete or move files — list them under `DELETIONS REQUESTED:`. This card requests none.
3. Writes confined to `/workspace`; never touch `komun_a` or Session A's worktree.
4. No `DELETE`/`TRUNCATE`/`DROP` against `komun_b`.
5. A warning claim must come from the command that emits it, and clippy must be forced to re-emit (`touch crates/server/src/main.rs`).
6. Report ends with `FILES WRITTEN: / GATES RUN: / BLOCKED: / DELETIONS REQUESTED:`.

## Files you own here
`crates/server/src/api/directory.rs`, `crates/server/src/api/node.rs`, `crates/server/src/tasks/registration.rs`,
`crates/server/src/config.rs`, `migrations/002_directory_open_registration.sql` (new), `config.example.toml`.
Read-only: everything else, especially `crates/server/src/auth/**` and all of `web/**`.

## Gate
```bash
docker exec -w /workspace agent-repo-agent-b bash -c 'export PATH=/usr/local/cargo/bin:$PATH; cd /workspace
cargo build --workspace --all-targets
cargo test --workspace
touch crates/server/src/main.rs; cargo clippy -p komun-server --no-deps --message-format short | grep "^crates/"'
```
- Build and tests exit 0. Your branch's test baseline is core 20 + server 25 (B3 measured 25); state the new counts and why they moved.
- **Clippy: your baseline is 9 rows** (`auth/mod.rs:98`, `auth/mod.rs:610`, `config.rs:157`, `config.rs:191`, `db/communities.rs:153`, `db/endorsements.rs:59`, `api/endorsements.rs:95`, `repl.rs:20`, `repl.rs:125`). A's A2a zeroed six of those on its own branch; they are not yours. **Your criterion: 9, no tenth** — name any new row and its file. Do not add `#[allow]`.
- **Runtime gate, with the server on port 3012** (`komun_b` is yours; copy `/workspace/config.toml`, change the port, run from a scratch cwd):
  1. with `open_registration` unset and `[registration] mode = "invite"`, the directory still accepts a peer registration → 200;
  2. with `open_registration = false` explicitly, the same call is refused;
  3. `GET /api/node` shows the resolved `open_registration`;
  4. a peer payload that omits `open_registration` still registers (old-peer compatibility);
  5. `psql "postgres://komun:komun@komun-db-b:5432/komun_b" -c "select id, open_registration from directory_entries"` shows the stored value;
  6. the migration applied cleanly on an existing `komun_b` — quote the `_sqlx_migrations` rows.
- **Ordering hazard:** A's branch edited `api/directory.rs`, `api/node.rs` and `tasks/registration.rs` too (its A3 hub diffs). Keep your diff surgical and confined to the concern above — a large rewrite of those files turns the eventual merge into a hand-resolution.

## Exit criteria
All six runtime observations with raw output; build/tests/clippy at the stated numbers; the new migration
listed in `FILES WRITTEN:` and present in `_sqlx_migrations` on `komun_b`; nothing outside your file list touched.
